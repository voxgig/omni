# omni boru port — test entry point.
# Run from the boru/ port directory:
#   boru run -no-check -no-compile test/main.boru
# (The suite lives in run.boru; this top level stays minimal — module
# namespace calls are the engine's well-behaved path.)
import "./test/run.boru" end
(Run.main (flex [])) drop
